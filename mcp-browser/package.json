{
  "name": "oberskills-browser",
  "private": true,
  "type": "module",
  "engines": { "bun": ">=1.3.0" },
  "scripts": {
    "start": "bun run src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test --timeout 60000",
    "test:live": "RUN_LIVE_EVALS=1 bun test --timeout 300000"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "puppeteer-core": "^24.10.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.0"
  }
}
