{
  "name": "oberskills-skill-eval",
  "private": true,
  "type": "module",
  "engines": { "bun": ">=1.3.0" },
  "scripts": {
    "start": "bun run src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test --timeout 60000",
    "test:live": "RUN_LIVE_EVALS=1 bun test test/smoke.live.test.ts --timeout 300000"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@anthropic-ai/claude-agent-sdk": "0.3.170",
    "zod": "^4.3.6",
    "yaml": "^2.8.1",
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.0"
  }
}
